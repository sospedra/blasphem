---
import { resolve } from "node:path";
import { pathToFileURL } from "node:url";
import type { Judgement, JudgeOptions } from "blasphem";
import { LANGUAGES } from "../lib/languages";
import { smoke } from "../lib/reports";
import Numeral from "./Numeral.astro";
import Spread from "./Spread.astro";

const example = smoke.languages.ES.cases.find((entry) => entry.case_id === "supplied-es-toxic");
if (!example) throw new Error("the smoke report lacks supplied-es-toxic");
const codes = LANGUAGES.map((language) => language.code).join(", ");

// Run the real package at build time so the sample cannot drift from it.
// The import stays dynamic and absolute so Vite leaves the module alone;
// bundling it strips the top-level await that instantiates the wasm.
const entry = pathToFileURL(resolve(process.cwd(), "../../packages/blasphem/dist/index.js")).href;
const { judge } = (await import(/* @vite-ignore */ entry)) as {
  judge: (text: string, options?: JudgeOptions) => Judgement;
};
const verdict = judge(example.text, { locales: ["es"], detectLanguage: false, grawlix: true });

const fields = [
  ["safe", "boolean", "True when no nudge is due. Also true when no locale routed the text."],
  ["score", "number 0 to 1", "Ordinal. Higher means more evidence. Not a probability, not calibrated across languages."],
  ["locale", "string or null", "The locale that produced the score. Null when nothing routed the text."],
  ["grawlix", "string or null", "The masked text, when the grawlix option is set. Null otherwise."],
] as const;

const options = [
  ["locales", "string[]", "Locales to load, lowercase. Defaults to all 15."],
  ["detectLanguage", "boolean", "Route by detected language. Defaults to true."],
  ["grawlix", "boolean", "Return the masked text. Defaults to false."],
] as const;
---
<Spread id="rite" label="the rite" class="ch rite">
  <div class="page r mirror">
    <Numeral value="II" class="two" />
    <p class="rubric">chapter two</p>
    <h2 id="rite-title" class="title">One import, one call</h2>
    <p class="copy">Import once. Then call <code>judge()</code> as often as the keystrokes come. The same bytes judge the same text the same way in every browser and in Node.</p>
    <p class="copy">With <code>detectLanguage</code> the router identifies the language before the toxicity check. Text it cannot route returns <code>locale: null</code> and <code>safe: true</code>. Nothing is judged. The nudge fails open by design.</p>
    <p class="copy">There is nothing to free. <code>judge()</code> returns a plain object and reuses one judge per option set.</p>
    <table class="ledger fields">
      <caption>Judgement</caption>
      <thead><tr><th>field</th><th>type</th><th>meaning</th></tr></thead>
      <tbody>
        {fields.map(([name, type, meaning]) => (
          <tr><td>{name}</td><td class="us">{type}</td><td class="meaning">{meaning}</td></tr>
        ))}
      </tbody>
    </table>
  </div>
  <div class="page l">
    <pre><code><span class="c">// One import. The module loads itself, so judge() is synchronous.</span>
<span class="k">import</span> &#123; judge &#125; <span class="k">from</span> <span class="s">"blasphem"</span>;

<span class="k">const</span> v = judge(<span class="s">{JSON.stringify(example.text)}</span>, &#123;
  locales: [<span class="s">"es"</span>],
  detectLanguage: <span class="k">false</span>,
  grawlix: <span class="k">true</span>,
&#125;);

v.safe    <span class="c">// {String(verdict.safe)}</span>
v.score   <span class="c">// {verdict.score}, ordinal</span>
v.locale  <span class="c">// {JSON.stringify(verdict.locale)}</span>
v.grawlix <span class="c">// {JSON.stringify(verdict.grawlix)}</span></code></pre>
    <p class="evidence">values from {smoke.evidence_status.replaceAll("_", " ")}</p>

    <h3 class="sub">Install</h3>
    <p class="copy">The package is private and unpublished. Build it from a clone of <a href="https://github.com/sospedra/blasphem">sospedra/blasphem</a>:</p>
    <pre><code>pnpm install --frozen-lockfile
pnpm --filter blasphem run build
pnpm --filter blasphem pack</code></pre>
    <p class="copy">Then <code>pnpm add ./blasphem-0.1.0.tgz</code> in your app, or reference the package as <code>workspace:*</code> inside this repository. The same entry runs in Node and in the browser. Serve <code>blasphem_bg.wasm</code> as <code>application/wasm</code>.</p>

    <h3 class="sub">Tongues</h3>
    <p class="copy"><code>{codes}</code>, lowercase, in <code>locales</code>. <code>id</code> is accepted as an alias for <code>ms</code>. Any other value throws. Every option is optional.</p>
    <table class="ledger fields">
      <caption>JudgeOptions</caption>
      <thead><tr><th>option</th><th>type</th><th>meaning</th></tr></thead>
      <tbody>
        {options.map(([name, type, meaning]) => (
          <tr><td>{name}</td><td class="us">{type}</td><td class="meaning">{meaning}</td></tr>
        ))}
      </tbody>
    </table>
  </div>
  <div class="spine" aria-hidden="true">one module &middot; deterministic &middot; offline after load</div>
</Spread>

<style>
  pre {
    margin: 0 0 18px;
    padding: 18px;
    overflow-x: auto;
    border: 1px solid var(--hair);
    background: #080707;
    font-family: var(--font-mono);
    font-size: 12.5px;
    line-height: 1.85;
    color: #c3bcb4;
    tab-size: 2;
  }
  pre .k {
    color: var(--blood-ink);
  }
  pre .s {
    color: #e0a49e;
  }
  pre .c {
    color: var(--taupe);
    font-style: italic;
  }
  .sub {
    font-family: var(--font-goth);
    font-weight: 400;
    font-size: 26px;
    margin: 34px 0 8px;
    color: var(--bone);
  }
  :global(.rite) {
    min-height: 640px;
  }
  :global(.rite) .page.l {
    padding-top: 84px;
  }
  pre code {
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }
  .fields th,
  .fields td {
    text-align: left;
    vertical-align: top;
  }
  .fields .meaning {
    font-size: 14px;
    color: var(--parchment);
  }
  .fields caption {
    text-align: right;
  }
  .page.r.mirror .fields {
    text-align: left;
  }
  .spine {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%) rotate(180deg);
    writing-mode: vertical-rl;
    font-family: var(--font-rubric);
    font-size: 15px;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--bone);
    background: var(--pitch);
    padding: 14px 0;
    z-index: 3;
    white-space: nowrap;
  }
  @media (max-width: 1023px) {
    :global(.rite) {
      min-height: 0;
    }
    :global(.rite) .page.l {
      padding-top: 64px;
    }
    .spine {
      display: none;
    }
    pre {
      font-size: 11.5px;
    }
  }
</style>
