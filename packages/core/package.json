{
  "name": "@blasphem/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contract, locale table, manifest parsing, and load policy. Never published: each package copies src/ at build time.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": "24.18.0",
    "pnpm": "11.13.0"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
