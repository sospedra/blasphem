import type { BundleConfiguration } from "./core/index.js";

// The build integration replaces this module with the consuming application's selection.
export const BUNDLE: BundleConfiguration | null = null;
