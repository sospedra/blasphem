{
  "name": "@blasphem/core",
  "version": "2.0.0",
  "private": true,
  "description": "Shared contract, locale table, manifest parsing, and load policy. Never published: each package copies src/ at build time. Blasphem hashes word and character n-grams into sparse feature vectors. A linear classifier trained offline scores them with 16-bit weights. Lexicons and context rules contribute to the verdict. Detection runs locally without neural networks or cloud inference.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": "24.18.0",
    "pnpm": "11.13.0"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
